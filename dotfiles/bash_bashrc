#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '
export EDITOR='nvim'
export MANPAGER=' nvim +Man!'
set -o vi

=======
export PATH=$PATH:$HOME/.dotfiles/scripts
export PATH=/home/kai/.nimble/bin:$PATH
